$('#form-pay').validator().on('submit', function(e) {
    if (e.isDefaultPrevented()) {
        // Se muestran mensajes de error
    } else {
        // Se muestra una ventana modal
        // Actualmente no se muestra ya que el flujo no debe de ser continuo segun el requerimiento.
        $('#preview').modal('show');
    }
})